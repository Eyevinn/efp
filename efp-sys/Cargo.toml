[package]
name = "efp-sys"
version = "0.2.5"
edition = "2021"
description = "FFI bindings to the Elastic Frame Protocol (EFP) C library"
license = "MIT"
links = "efp"

[build-dependencies]
bindgen = "0.71"
cmake = "0.1"
